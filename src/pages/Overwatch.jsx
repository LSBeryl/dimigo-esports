import styled from "styled-components";
import { Col } from "../components/atomic";

export default function Overwatch() {
  return <Wrapper padding="1rem">오버워치</Wrapper>;
}

const Wrapper = styled(Col)``;
